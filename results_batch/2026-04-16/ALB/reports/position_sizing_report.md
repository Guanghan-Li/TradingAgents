### ALB Position Sizing
ALB merits a `medium` conviction sizing plan rather than a full-risk allocation. Price action is strong: the stock closed at `215.62` on `2026-04-16`, essentially at its `52-week high` with a large volume expansion, and it has climbed well above both the `50-day` (`171.94`) and `200-day` (`122.33`) averages. But the setup is also extended and volatile, with `ATR` around `10.52` and `beta` at `1.43`, while fundamentals remain mixed because trailing `EPS`, `net income`, and `profit margin` are still negative even though forward earnings and free cash flow are positive.

Use a staged entry. A reasonable plan is a `1.0%` starter position and a `2.5%` full target weight, with a total trade loss budget capped at `0.30%` of portfolio NAV. That keeps risk controlled if the breakout fails after a sharp run, while leaving room to add only if ALB holds the breakout and volatility contracts or price confirms above the recent surge area.

```json
{
  "conviction": "medium",
  "target_weight_pct": 2.5,
  "initial_weight_pct": 1.0,
  "max_loss_pct": 0.3,
  "sizing_rationale": "ALB has strong momentum and a fresh breakout to a 52-week high, but elevated ATR, high beta, and still-weak trailing profitability argue for moderate rather than aggressive sizing. A 1.0% starter and 2.5% max position balances trend participation with discipline, while keeping the trade-level loss budget tight at 0.30% given the stock's recent volatility and extended move."
}
```