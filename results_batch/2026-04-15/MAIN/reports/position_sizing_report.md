## MAIN Position Sizing

- Conviction: `medium`
- Target weight: `2.5%`
- Starter weight: `1.0%`
- Max loss budget: `0.5%` of portfolio

MAIN screens well on income and quality for a BDC-style financial: about `10.5x` TTM earnings, `5.53%` dividend yield, `17.0%` ROE, and `0.81` beta. The sizing stays moderate because the tape is only partially repaired: MAIN fell from the upper-`50s` to `51.28` in late March, has rebounded to `57.83`, reclaimed the `50-day` average (`56.77`), but is still below the `200-day` average (`60.62`). ATR is `1.57`, so a roughly `2x ATR` stop near `54.7` sits under recent support and argues for starting smaller, then adding only if the rebound holds.

```json
{
  "conviction": "medium",
  "target_weight_pct": 2.5,
  "initial_weight_pct": 1.0,
  "max_loss_pct": 0.5,
  "sizing_rationale": "MAIN has attractive valuation, yield, and profitability metrics, but recent price action still reflects a recovery rather than a fully re-established uptrend. With ATR near 1.57 and price at 57.83, a 2x ATR stop around 54.7 supports a moderate initial allocation and a capped full position size, adding only if price continues to hold above the 50-day average and momentum remains constructive."
}
```